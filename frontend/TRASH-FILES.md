## Trash log

- `src/styles.css` — moved to `TRASH/` — replaced by `src/styles.scss` (ATD design tokens).
- `src/app/core/api.service.ts` — moved to `TRASH/` — split into `core/api/{market,dataset,account,seller}-api.service.ts` per ATD naming.
- `src/app/features/markets/market-picker.component.ts` — moved to `TRASH/` — replaced by `features/markets/market-picker-page/` folder.
- `src/app/features/admin/admin-import.component.ts` — moved to `TRASH/` — replaced by `features/admin/admin-import-page/` folder.
- `src/app/features/map/map-page.component.ts` — moved to `TRASH/` — split into `features/map/map-page/` + child components.
